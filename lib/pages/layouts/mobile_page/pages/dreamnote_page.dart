import 'package:flutter/material.dart';

class DreamNote extends StatelessWidget {
  const DreamNote({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        height: 100,
        width: 100,
        color: Colors.red,
        child: Text("icbds"),
      ),
    );
  }
}