import 'package:flutter/material.dart';

class MarmitonPage extends StatelessWidget {
  const MarmitonPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        height: 100,
        width: 100,
        color: Colors.orange,
        child: Text("icbds"),
      ),
    );
  }
}