import 'package:flutter/material.dart';

class BudgetZeroPage extends StatelessWidget {
  const BudgetZeroPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        height: 100,
        width: 100,
        color: Colors.purple,
        child: Text("icbds"),
      ),
    );
  }
}