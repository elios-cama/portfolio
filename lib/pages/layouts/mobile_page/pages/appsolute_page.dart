import 'package:flutter/material.dart';

class AppsolutePage extends StatelessWidget {
  const AppsolutePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        height: 100,
        width: 100,
        color: Colors.green,
        child: Text("icbds"),
      ),
    );
  }
}