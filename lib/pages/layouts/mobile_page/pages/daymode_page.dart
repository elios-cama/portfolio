import 'package:flutter/material.dart';

class DaymodePage extends StatelessWidget {
  const DaymodePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        height: 100,
        width: 100,
        color: Colors.blue,
        child: Text("icbds"),
      ),
    );
  }
}
